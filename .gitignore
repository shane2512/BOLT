.env
.env.local
.mcp.json
node_modules/
dist/
.next/

# Build artifacts and codegen output — regenerated by pnpm contracts:build / subgraph:codegen
contracts/artifacts/
contracts/cache/
subgraph/build/
subgraph/generated/

# Throwaway Phase 0 spike — its own package-lock/node_modules, not part of the workspace
docs/evidence/phase0-spike/node_modules/

# Phase 6 ceremony state — contains the demo key quorum private keys.
docs/evidence/phase6-state.json
