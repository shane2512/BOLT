# subgraph

Placeholder. Phase 4 owns this directory — manifest, schema, and AssemblyScript
mappings for every `BoltRegistry` event, plus USDC `Transfer` indexing.

**There is no documentation MCP for The Graph.** Per `CLAUDE.md` Rule 0, before
writing a manifest, schema, or mapping here: stop and ask which documentation
is needed, naming precisely what you need to know. Do not proceed from memory.
